# list
data_list = [0, 1, 2, 3, 4, 5]

# tuples: (nialai konstan)
# - tidak bisa di assignment atau diubah
# - tidak bisa di apppend
data_tuples = (0, 1, 2, 3, 4, 5)

# sets: koneksi tanpa index (kumpulan data(himpunan))
daata_sets = {0, 1, 2, 4, 5, 6}
